import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  fadedDividerTextContainer: {
    flexDirection: 'row',
    gap: 12,
  },
  fadedDividerText: {
    color: '#525252',
    fontSize: 12,
    lineHeight: 20,
  },
});
